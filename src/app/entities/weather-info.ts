export class WeatherInfo {
    weatherText: string;
    celsiusDegrees: string;

constructor( weatherText: string,
    celsiusDegrees: string) {
        this.weatherText = weatherText;
        this.celsiusDegrees = celsiusDegrees;
    }
}
